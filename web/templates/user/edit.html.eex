<h2>Edit user</h2>

<%= render "form.html", conn: @conn, changeset: @changeset, roles: @roles,
                        action: user_path(@conn, :update, @user) %>

<%= link "Back", to: user_path(@conn, :index) %>
